#!/usr/bin/env bash
#
echo "usage: $0 {{comments}}"
comments=$@

if [[ -z "$comments" ]]; then
  comments="commit as of $(date)"
  echo "auto set: $comments"
else
  echo "$comments"
fi

git add .
git commit -m "$comments"
git push
