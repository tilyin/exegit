#!/usr/bin/env bash
#
echo "usage: $0 {{comments}}"
comments=$@

if [[ -z "$comments" ]]; then
  comments="commit as of $(date -u --rfc-3339=s)"
  echo "auto set: $comments"
else
  echo "$comments"
fi

git add .
git commit -m "$comments"
git push
